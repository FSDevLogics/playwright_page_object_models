[project]
name = "playwright-page-objects-models"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest-playwright>=0.9.0",
]

[project.scripts]
playwright-page-objects-models = "playwright_page_objects_models:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
